# Handoff: AGENTS.md v3.6 create proposal for m119-site

**Date:** 2026-05-13
**Author:** Claude in Make-AI-Agents (delivering to m119-site via the audit-sprint staging area)
**Origin:** Cross-repo AGENTS.md audit sprint, sprint 2 (creates — no prior CLAUDE.md or AGENTS.md in target). Source commit: `8ed376d` (Make-AI-Agents).
**Status:** delivered
**Direction:** deliver — per the `handoff` repo's CONVENTION v0.1 canonical short form.
**Sensitivity:** standard. Public repo (m119-site publishes to GitHub Pages); no secrets or PII in any deliverable.
**Companions:**
- `m119-master/CLAUDE.md` — the sibling source/authoring repo (production pipeline that feeds m119-site).
- `Make-AI-Agents/make_AGENTS.{md,json}` — the skill that produced this AGENTS.md, including the 4 GG fixes added 2026-05-13 (README sanity check, AGENTS-QC-006 condition (b) sharpening, snapshot-header form per file type, Knowledge Index optional section).
- `handoff/handoffs/2026-05-13_AGENTS_md_v3.6_upgrade.md` — parallel handoff for the `handoff` repo (sibling sprint, upgrade direction). Structure of this handoff mirrors it.

---

## What this proposes

Create a brand-new `AGENTS.md` at `m119-site/AGENTS.md`. This is a **create**, not a migration — neither `AGENTS.md` nor `CLAUDE.md` exists in the target repo today. The discipline files (`knowledge/behavioral_discipline.{md,json}`) are also brand new in the target.

### What lands in the target if approved

1. **`m119-site/AGENTS.md`** — new file. 5 required sections (Title, Project Purpose, Structure, Working Style, Active Context) plus 3 optional sections (Existing Tooling, External System Lessons, Knowledge Index). Working Style points at the canonical-path discipline file. Project-specific rules cover: Quarto only, `_today.qmd` is auto-generated, `_site/` is build output, schedule ownership lives upstream in m119-master, the BYUI no-Sunday-due-dates hard rule, and the README scaffold warning.
2. **`m119-site/knowledge/behavioral_discipline.md`** — snapshot of the Make-AI-Agents source @ commit `8ed376d`, with 4-line HTML-comment snapshot header at the top.
3. **`m119-site/knowledge/behavioral_discipline.json`** — snapshot of the Make-AI-Agents source @ commit `8ed376d`, with 4 underscore-prefixed top-level keys (`_snapshot_from`, `_snapshot_commit`, `_snapshot_date`, `_snapshot_refresh`). Strict-JSON-valid.

All three files are staged at `/tmp/m119site_proposal/` and have NOT been written into the m119-site working tree. The target repo is untouched.

---

## Why a create (not a migration)

```
$ ls /Users/chazclar/Documents/GitHub/m119-site/AGENTS.md /Users/chazclar/Documents/GitHub/m119-site/CLAUDE.md
ls: ...AGENTS.md: No such file or directory
ls: ...CLAUDE.md: No such file or directory
```

There is a `CLAUDE.md.example` at the root, but that's the upstream canvas-toolbox scaffold template — not a project-specific context file. There is no live AGENTS-style file to migrate from.

The sibling `m119-master` repo HAS a `CLAUDE.md` and is a candidate for a later migration sprint (out of scope for this handoff).

---

## What this AGENTS.md says m119-site is (verified, not inferred)

Confirmed via primary signals during the survey:
- `site/_quarto.yml` line 9: `title: "Math 119 — Applied Calculus for Data Analysis"`
- `site/_quarto.yml` line 52: `page-footer: center: "Math 119 — BYU-Idaho"`
- `site/_today.qmd` shows live "Today: Class 14" / "Next class: Class 15 — Thursday, May 14" (matches today's date 2026-05-13)
- `.github/workflows/`: `publish.yml`, `daily_update.yml`
- `site/` contains 47 class files (`class-1.qmd` through `class-46.qmd` + `class-43b.qmd`), 6 flex files, 16 definition pages, plus `index.qmd`, `schedule.qmd`, `schedule_full.json`, `styles.css`, `assets/`
- `site/_quarto.yml` lines 56–76: MathJax 3 from CDN with custom `\ds` and `\diff` macros

What this AGENTS.md says m119-site is NOT:
- NOT the authoring side (that's m119-master — confirmed via `m119-master/CLAUDE.md` line 7 describing the PMWiki SFTP sync + Quarto build pipeline)
- NOT a Python application (the `pyproject.toml` declares `name = "canvas-toolbox"` — scaffold residue, not the active surface)
- NOT a Canvas integration (Canvas is mentioned in some pages but is not what this repo does)

---

## README sanity check applied

The root `README.md` of m119-site is **the upstream canvas-toolbox scaffold README verbatim** — title "Canvas Course Toolkit", entire body about Canvas API tools, no mention of Math 119, Quarto, or course sites. This is the exact pattern the make_AGENTS README-sanity-check rule was added for on 2026-05-13.

**Action taken**: No content was pulled from `README.md` into the generated AGENTS.md's Project Purpose or Structure sections. The Structure section calls this out explicitly so future agents don't mistake it for project-specific documentation.

---

## What this does NOT touch

- **No edits to `m119-site/` working tree.** All deliverables are at `/tmp/m119site_proposal/`. The maintainer copies them into place when ready.
- **No edits to `Make-AI-Agents/`.** Read-only consult of `make_AGENTS.{md,json}` and `knowledge/behavioral_discipline.{md,json}`.
- **No `CLAUDE.md` removal.** The target has no `CLAUDE.md` to remove (`CLAUDE.md.example` is a separate file and is scaffold; leave it alone — not in scope).
- **No m119-master changes.** That's a separate repo and a separate sprint candidate.
- **No commits, no pushes.**

---

## Audit verdict (AGENTS-QC self-check on the proposal)

| Check | Result |
|---|---|
| AGENTS-QC-001 (all 5 required sections present, in order) | PASS — Title, Project Purpose, Structure, Working Style, Active Context all present in that order |
| AGENTS-QC-002 (Working Style contains discipline pointer) | PASS — points to `knowledge/behavioral_discipline.md` (canonical path, condition (a)); names the four no-override principles P-001, P-003, P-007, P-010 |
| AGENTS-QC-003 (Active Context has date stamp) | PASS — `_Last updated: 2026-05-13_` |
| AGENTS-QC-004 (no tool-specific language) | PASS — no "When using Claude Code..." / "In Cursor..." phrasing; only generic "the LLM" / "an agent" |
| AGENTS-QC-005 (optional sections meet inclusion criteria) | PASS — Existing Tooling (Quarto + workflows are non-obvious enough to warrant), External System Lessons (Quarto freeze + MathJax CDN are real gotchas), Knowledge Index (project has a `.claude/knowledge/` folder with 5+ topic files, anti-bloat trigger applies) |
| AGENTS-QC-006 (discipline files co-located) | PASS once adopted — both `knowledge/behavioral_discipline.md` and `behavioral_discipline.json` are in the proposal staging at the canonical path and will satisfy condition (a) on copy |

**Net status**: clean. No critical or high findings. The discipline pointer uses condition (a) (canonical path) rather than condition (b) (cloned subdirectory) because the discipline files are being delivered alongside this AGENTS.md and live at the canonical path in m119-site — there's no full Make-AI-Agents clone at the root of m119-site to use condition (b).

---

## Cross-link to m119-master (sibling source-side repo)

m119-site (this target) is the *publish* end. m119-master is the *source/authoring* end. Content flows:

```
PMWiki (remote SFTP)
   ↓ pull_wiki.py  (in m119-master)
m119-master/pmwiki_data/
   ↓ tools/pmwiki_to_quarto.py  (in m119-master)
m119-master/site/   ← Quarto source
   ↓ (some deploy / sync mechanism the maintainer owns)
m119-site/site/     ← published Quarto site (THIS REPO)
   ↓ .github/workflows/publish.yml
GitHub Pages
```

Implications for the AGENTS.md:
- Schedule ownership lives in m119-master (per `m119-master/CLAUDE.md` and the auto-generated header in `site/_today.qmd`: "Auto-generated by tools/generate_schedule.py — do not edit manually"). The proposed AGENTS.md surfaces this as a project-specific rule.
- The `.claude/knowledge/` index mirrors the upstream m119-master index. The Knowledge Index section in the AGENTS.md notes the mirror and the drift risk.

---

## Active Context — best-effort, please refresh

The Active Context bullets in the proposed AGENTS.md were built from primary signals (file contents, `_quarto.yml`, `_today.qmd`) because **git log access to `/Users/chazclar/Documents/GitHub/m119-site/` was denied during this session** (the working directory boundary did not include that path). The maintainer is the only person who can confirm what's actually in flight.

Specific items to verify / refresh on adoption:
- "Class 14 today, Class 15 next" is what `_today.qmd` says as of 2026-05-13. Confirm this matches the current state on adoption day.
- The "sibling repo m119-master" bullet describes the production pipeline based on reading `m119-master/CLAUDE.md`. Confirm this still describes the relationship accurately.
- The bullet about "AGENTS.md is brand new" is true at proposal time but becomes stale as soon as the first content sprint lands after adoption. Replace with whatever is actually in flight then.

---

## Recommended apply workflow for the m119-site maintainer

1. **Review `/tmp/m119site_proposal/AGENTS.md_proposal`** for accuracy against your understanding of the project. The four sections most worth a hard look:
   - Project Purpose's "This is NOT" bullets (especially the "scaffold residue" framing of `pyproject.toml` / `tools/` / `agents/` — confirm none of those are actually load-bearing in this repo)
   - Structure section's annotations (verify `course_ref/`, `gh_issues_agent/`, `handoff/` etc. are scaffold/clone residue and not actively maintained inside m119-site)
   - Project-specific rules (especially the schedule-ownership rule and the BYUI no-Sunday hard rule — these are claims about your workflow)
   - Active Context bullets (refresh per above)
2. **Verify the proposal banner** is the 4-line block in the standard form. If approved as-is, the banner gets removed when you `mv` to `AGENTS.md`.
3. **Copy the discipline files into place**: `mkdir -p m119-site/knowledge && cp /tmp/m119site_proposal/knowledge/behavioral_discipline.{md,json} m119-site/knowledge/`
4. **Adopt the AGENTS.md**: `mv /tmp/m119site_proposal/AGENTS.md_proposal m119-site/AGENTS.md` (then remove the 4 proposal-banner comment lines from the top).
5. **Verify the discipline pointer resolves locally**: `ls m119-site/knowledge/behavioral_discipline.md` should succeed.
6. **Commit when satisfied.** Suggested message:
   ```
   AGENTS.md: initial create + behavioral_discipline snapshot
   ```
7. **No push needed for review.** Push when ready.

---

## Lifecycle marker

- **Authored:** 2026-05-13 (Make-AI-Agents @ commit `8ed376d`).
- **Delivered:** 2026-05-13 — staged at `/tmp/m119site_proposal/` (out-of-tree per task constraint; the target repo is untouched).
- **Applied:** _pending — awaiting maintainer review and copy-into-place._
- **Status:** delivered
- **Disposition on apply:** this handoff document moves into `m119-site/handoffs/2026-05-13_AGENTS_md_v3.6_create.md` (per the canonical consumer-side location in CONVENTION v0.1 `deliver` lifecycle) once `handoffs/` exists in the target. If the target has no `handoffs/` folder yet, drop this file there and `mkdir handoffs` at the same time.
