# Environment variables
.env

# Downloaded PMWiki data
pmwiki_data/

# Python
__pycache__/
*.pyc
.venv/

# AI files
CLAUDE.md
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Sibling repos cloned locally (not tracked here)
canvas-toolbox/
gh-issues-agent/
handoff/
make-ai-agents/

# Orphaned — no longer linked from anywhere since RStudio references
# were replaced with Positron site-wide. Kept locally, not tracked.
site/projects/rstudio_install.qmd

# tools/check_links.py output — regenerate with `python tools/check_links.py`;
# external link status is a live network snapshot, not durable content.
link_check_report.csv

# macOS
.DS_Store

# Quarto build output — re-rendered by CI and deployed to the gh-pages
# branch; never served from main. See AGENTS.md ("site/_site/ is build output").
site/_site/
site/.quarto/

# R
.Rhistory
.RData
.RDataTmp
.Ruserdata
.Rproj.user/
*_cache/
*.utf8.md
*.knit.md
.Renviron
rsconnect/
